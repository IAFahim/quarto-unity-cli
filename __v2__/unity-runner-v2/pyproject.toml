[project]
name = "quarto-unity-runner"
version = "2.0.0"
description = "WebSocket bridge for live Unity code execution in Quarto documents"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "websockets>=12.0",
]
